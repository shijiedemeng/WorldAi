- 工作流画布新增悬空连线创建子模块：从现有节点拖线到空白处松开时，前端弹出新建子模块弹窗；保存后自动把新子模块作为来源节点的下一步并保存工作流关系。
- 工作流保存新增脱节节点校验；工作流子模块新增人工审核开关，需要审核且未通过的节点不会自动下发，页面审核通过后再触发 ready 任务下发。
- 子模块从 `requirement_info` 拆到独立 `requirement_module_info` 表；后端 API 保持 `RequirementResponse` 兼容，写操作按总需求/子模块分别落表，`ai-api-agent-integration` 的 `agents next` 会合并无链路子模块任务。
