- 优化子模块执行状态：服务端下发任务应在客户端回调 `COMMAND_STARTED` 时把关联链路置为 `DOING`，由链路同步子模块为 `IN_PROGRESS`；无链路任务直接把子模块置为 `IN_PROGRESS`，完成后按结果改为 `DONE/BLOCKED`。
- 智能体通过技能主动取任务时，必须用 `agents next --claim`，非 ACP 会话会先认领当前子模块和链路；ACP 会话由下发链路负责状态，不在技能里重复认领。
- 需求详情子模块列表新增“进度”查看和执行中“重置”入口；重置只允许 `IN_PROGRESS` 子模块回到 `PENDING`，并把正在处理的开发链路退回 `TODO`。
- 修正执行回执策略：回执用于回写 ai-api 的 `executionDetails`，不应在工作目录生成 `receipt.md` 或 `receipt_*.md`；已把历史 `/Users/zhaoyiming/Desktop/项目/ce/receipt_cs-1-2.md` 移到废纸篓。
- 修正长会话技能初始化策略：技能和项目 Markdown 准备在客户端会话创建阶段完成，执行任务时复用会话缓存的 `initializedSkills`，不再在每个任务 prompt 中提示重新加载/初始化技能。
- 项目文档库关联新增开发文档作用域 `DEVELOPMENT_DOCUMENT`，需求/缺陷/开发文档关联都支持选目录后递归加载子文档；开发文档会同步到 `.ai-api-context/development-documents.md`，ACP 会话初始化时注入为长期上下文。
- 文档库 UI 调整为左侧只显示目录树，目录节点提供新增子目录、删除、详情/修改；右侧列表按当前目录分页查询并只展示直接子文档，目录不再混入文档列表。
- 生图调用超时改为 yml 配置，默认读超时 30 分钟；b64_json 或大响应原文保存到本地响应目录，数据库只保存摘要和本地文件路径，前端响应详情隐藏历史 b64 大内容。
- 生图记录新增失败重试：仅 `FAILED` 状态显示重试按钮，复用原记录参数重置为 `PENDING` 后重新异步提交，不新增记录。
- 重做需求工作流模式：正常模式不变；工作流模式下子模块作为流程节点，通过 `requirement_workflow_edge` 前置/下一步关系编排，后端按“全部前置完成才就绪”自动下发，前端详情页改为工作流编排板并支持编辑流程边。
- 工作流编排前端改用 `@vue-flow/core`，支持拖线新增流程关系、删除关系、保存流程；工作流模式的新建子模块改为画布工具栏弹窗创建，并继承总需求执行模式。
- 工作流 UI 简化：去掉节点级“单独执行”，保留总需求级“从头开始执行”并由后端从断点继续；右键画布空白处可快速创建子模块；节点编辑改为当前页弹窗。
