- 新增 Codex ACP 适配到 python-client，接入方式是 `codex app-server --listen stdio://`，不是 `--acp`。
- `command_runner` 已按 `worker_command` 自动分流到 Codex / Qoder 的 ACP 实现，Codex 会走 thread/turn 的 JSON-RPC 会话。
- 当前沙箱里直接烟测 `codex app-server` 遇到 PATH 更新 `Operation not permitted`，所以这次只完成了代码级和语法级验证，端到端运行还要在目标机器上复核。
- 处理 `改动/020.md`：需求 AI 分析改为后台异步执行，新增 `ANALYZING` 状态锁定页面并自动轮询，分析完成后再刷新真实状态。
- 缺陷分析改成通过 AI 模型配置生成分析结果，并在角色描述里强调缺陷需求分析和 URL/地址阅读；缺陷文本在展示、分析和推需求时清理 HTML 标签与实体。
- 补充修正：`ANALYZING` 只能由需求 AI 分析流程设置，需求新建、编辑、手动状态更新接口都禁止手工传入该状态。
- 云效缺陷列表的状态筛选改为多选，多个状态 ID 用逗号串提交，后端沿用已有拆分逻辑生成云效 `status CONTAINS` 数组条件。
- 修正 AI 处理中页面锁定：需求详情和缺陷 AI 处理不再使用全屏锁，只锁内容区，菜单和返回入口可继续使用。
- 新增 AI 分析记录日志模块，记录需求 AI 拆分和缺陷 AI 分析的模型请求、响应、状态和错误信息，前端在日志管理下双击查看完整内容。
- 修正 AI 日志可观测性：模型响应解析失败时保存 `statusCode/headers/body` 响应信封，并在 `assistant content` 缺失错误里附带原始响应摘要。
- 排查日志 ID 2：模型服务返回 200，但 `choices[0].message` 只有 `role=assistant` 没有 `content`，所以需求 AI 拆分无法解析 JSON、不会创建子模块；已给 GPT-5/推理模型请求增加输出 token、低推理强度和 JSON 输出约束。
- 排查日志 ID 3：同一模型代理在增加 JSON/推理参数后仍返回空 `content`；已增加 `FALLBACK` 兜底，HTTP 200 但不可解析时自动生成复现、修复、回归三个子模块并保留原始响应日志。
- 需求详情新增双击编辑需求说明：总需求必须无子模块且 `PENDING`，子模块沿用原有可编辑规则；保存走 `/api/requirements/{requirementNo}/description` 专用接口。
- 前端新增全局顶部 Tab 导航：路由切换自动登记/激活 Tab，按路由名 + 参数去重，缺陷列表不会重复打开，同一缺陷详情或 AI 处理重复打开会切到已有 Tab。
