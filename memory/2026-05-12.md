- 完成技能管理闭环：服务端新增 `skill_info`，支持技能包上传/下载、启停状态；Agent 支持选择 `skillCodes`。
- python-client 新增 `skills_dir` 配置，启动会话时会拉取服务端技能列表与 Agent 绑定技能，按本地技能目录动态匹配并回填 `initializedSkills`。
- 在线客户端仍保持运行态内存展示，但任务开始/完成会自动写入 `agent_session`，Agent 列表可查看执行过任务的会话记录。
- 新建技能已修正为“上传技能包”模型，技能主体按目录型压缩包处理，文本区仅作为补充说明，不再按单一文件内容建模。
