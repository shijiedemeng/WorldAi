- 处理 `改动/016.md`：云效缺陷源不再创建项目，也不再手填项目 ID；改成先拿组织和令牌，再自动拉取项目列表供下拉选择。
- 后续修正为：云效项目改成组织 + 令牌后自动拉取项目列表供下拉选择，后端新增 `/api/defects/yunxiao/projects`，前端不再手填项目 ID。
- 云效项目列表 403 `Forbidden.InvalidOrganizationMember` 的根因通常是 token 对应用户不在该组织或没有项目只读权限，不是项目列表接口本身拼接错误。
- 验证：前端 `npm run build` 通过；当前环境无 `mvn`，后端 Java 编译未能本地执行。
- 缺陷查看已拆成独立的缺陷列表页和缺陷详情/处理页；列表页按平台动态显示查询条件并按分页返回远程缺陷，源配置页只保留跳转入口。
- 缺陷详情页必须保留 AI 分析缺陷和推送到需求列表的处理链路；列表入口应明确标注查看/AI处理，避免误以为功能被删除。
- 处理 `改动/017.md`：云效缺陷筛选已独立前端组件，状态使用 `017-01.md` 完整字典，负责人/创建人通过云效项目成员接口下拉选择；新增已保存缺陷源的云效成员查询接口。
- 处理 `改动/018.md`：云效工作项 `description/content` 可能是 `htmlValue/jsonMLValue` 富文本 JSON，页面和 AI 分析前必须在后端归一化为纯文本，并从 `jsonMLValue` 提取图片链接。
- 处理 `改动/019.md`：缺陷列表改为双击行进详情，AI 处理独立到新页面；处理期间锁定页面，已推送的缺陷禁止重复推送；需求说明和执行步骤改为预览+双击展开全文。
- 补充修正：需求 AI 分析不是异步后台任务，必须在整页全屏锁定下执行，并给分析接口单独放宽超时，不能沿用默认 10 秒。
