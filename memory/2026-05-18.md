- 处理 `改动/024.md`：新增 AI 生图模块，复用系统 AI 模型配置作为生图接口配置源，通过 `supportImageFlag/enabledFlag` 过滤。
- 生图新增正面/负面提示词模板、异步生图记录、文生图 `/v1/images/generations`、图生图 `/v1/images/edits`，统一使用 `b64_json` 后保存到本地 `state/ai-images`，数据库只存路径和脱敏响应。
- 生图记录页通过状态轮询展示 `PENDING/RUNNING/SUCCESS/FAILED`，完成后可查看原图、mask、生成图片、最终提示词、请求和响应。
- 修正在线客户端默认会话重复创建：页面刷新和 pending 轮询不再因为 `FAILED` 默认会话反复下发 `CREATE_SESSION`，手工重试复用原 requestId；同时补充 `/Users/zhaoyiming/Desktop/项目/ce` 的 project-markdown-workspace 配置。
