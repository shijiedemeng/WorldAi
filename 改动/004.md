前置md:
   002 003
缺陷修复:
严重问题:
需求改动:
    修改技能如果查询需求执行的时候，目前其他ai是查不出来的 
 ▪ 需求 ai-api 当前状态：

- 标题: 修改内容
- 状态: PENDING
- 链路: 无（尚未创建任何开发链路）
- 缺项: DEVELOPMENT_LINK、BACKEND、API、DOCS、TEST_RECORD

技能应该是通过当前的Agent编号自动获取需要执行的子执行模块，按照执行顺序，如果执行了当前任务还有后续也要自动执行
ai修复情况: 已处理，本次修复的是 ai-api-agent-integration 技能和配套脚本：
1. 技能文档补充了“先按当前 Agent 自动拿可执行任务”的规则，明确其他 AI 不应只查 requirement/inspection，而应先按自己的 `agentCode` 拉当前待执行链路。
2. 技能脚本新增 `agents next` 命令，会基于当前 `agentCode` 自动拉取该 Agent 的任务，并结合 `dependsOnLinkId` 判断 `currentTask`、`readyTasks`、`waitingTasks`、`blockedTasks` 和执行顺序。
3. `agents tasks` 现在复用同一套执行计划逻辑，避免只能看到原始待办而不知道先做哪一个。
4. skill 文档新增要求：创建 Agent 时要带 `projectCode`；创建链路如果有执行顺序要求，必须带 `dependsOnLinkId`。
5. skill 文档新增要求：一个任务回写 `DONE` 后，要立即重新执行 `agents next`，如果还有 ready task，则继续自动执行下一条。
