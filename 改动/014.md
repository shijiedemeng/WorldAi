前置md:
   
缺陷修复:
严重问题:
需求改动:
   调整首先ai-api-agent-integration 和 project-markdown-workspace 这两个技能调整配置说明
   .skill-config.json 改为 skill-config.json 不在隐藏
   需要把.skill-config.json 新增工作目录，也就是按照工作目录来选择哪个配置，如果工作目录没有找到对应的配置，则需要提示，并且终止
   因为现在的skill-config.json我需要配置多个工作目录，多个ai，公用一个会出问题，所以需要按照工作目录，在单独配置，技能使用的时候，需要动态传入当前工作目录，然后匹配

ai修复情况:
1. 已将 `skills/ai-api-agent-integration/.skill-config.json` 和 `skills/project-markdown-workspace/.skill-config.json` 改为非隐藏的 `skill-config.json`，并按 `workspaces` 数组组织配置。
2. 两个 skill 的配置都新增 `workspaceDir`，当前已回填 `/Users/zhaoyiming/Desktop/项目/ai-api` 对应配置：
   - `ai-api-agent-integration`：`projectCode=ai-api`、`agentCode=ce-main-agent`、`agentRole=DEVELOPER`、`baseUrl=http://localhost:8080`
   - `project-markdown-workspace`：`projectCode=ai-api`、`agentRole=DEVELOPER`、`url=http://localhost:8080`
3. 脚本启动时会按工作目录匹配配置：
   - `ai-api-agent-integration` 支持 `--workspace-dir`、`AI_API_SKILL_WORKSPACE_DIR`、当前目录。
   - `project-markdown-workspace` 支持 `--workspace-dir`、`PROJECT_MARKDOWN_WORKSPACE_DIR`、当前目录。
   - 配置路径支持当前目录等于 `workspaceDir`，也支持当前目录位于该工作目录子目录内。
4. 如果当前工作目录匹配不到 `workspaces[].workspaceDir`，脚本会明确提示并以非 0 状态退出，避免多个项目/多个 AI 共用错误配置。
5. 已更新两个 `SKILL.md` 的配置说明和示例，全部改为 `skill-config.json`，并说明必须按工作目录匹配。
6. 已兼容 `--workspace-dir` 放在子命令前或后，例如：
   - `python3 skills/ai-api-agent-integration/scripts/ai_api_skill_client.py config show --workspace-dir /Users/zhaoyiming/Desktop/项目/ai-api`
   - `python3 skills/project-markdown-workspace/scripts/project_markdown_workspace.py config --workspace-dir /Users/zhaoyiming/Desktop/项目/ai-api`
7. 已验证：
   - `python3 -m compileall` 通过。
   - 两个 skill 在当前工作目录能正确读取配置。
   - 传入不存在的工作目录会提示未匹配配置并终止。
