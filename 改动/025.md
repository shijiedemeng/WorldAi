前置md:
缺陷修复:
严重问题:
需求改动:
    1:需求详情需要新增刷新按钮，用来刷新对应的数据
     2:需求编号 cs1 我已经给 cs-1-2 下达了执行任务，会话管理那边也可能看到，但是没有任何响应，包括记录也没有返回，
       你的会话更多是与后端的请求，但是我想要的是ai智能体之间的会话记录，而且不是请求后段的，然后实际上你发送，下达的应该是按照 ai-api-agent-integration 技能 处理什么需求，什么子需求，这样相关链路，ai才会返回
ai修复情况:
    1:已在需求详情页头新增“刷新”按钮，点击后会重新加载当前需求详情、子模块、开发链路和测试记录，避免执行后页面数据停留在旧状态。
    2:已调整下发执行提示词，明确要求 Agent 必须使用 ai-api-agent-integration 技能处理当前总需求/子模块；如果没有已有开发链路 ID，需要先为子模块创建开发链路，再执行并回写 DONE/BLOCKED，回写后还要再次查询子模块确认状态同步。
    3:已新增客户端智能体会话事件上报接口：python-client 在真实调用 CLI/ACP 前会记录 AGENT_PROMPT，拿到智能体输出后会记录 AGENT_RESPONSE，执行异常时记录 AGENT_ERROR。记录内容包含 commandId、requirementNo、linkId、prompt、stdout/stderr 或 ACP 返回文本，属于智能体实际收发内容，不再只是后端 WebSocket/HTTP 包装。
    4:在线客户端详情页已把原“会话实际收发历史”调整为“智能体实际会话历史”，主表只展示 AGENT_* 事件；原后端请求级别收发仍可通过单个会话的“查看收发记录”查看，用于底层排查。
    5:已放开客户端下发命令 linkId 必填限制，支持按子模块直接下发；没有 linkId 的命令由 Agent 依据 ai-api-agent-integration 创建并回写开发链路。
    6:验证情况：frontend 执行 npm run build 通过；python-client 执行 python3 -m compileall python-client/ai_api_client 通过；后端因当前环境没有 mvn 且无 mvnw，未执行 Maven 编译。
