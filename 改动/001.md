前置md:
缺陷修复:
严重问题:
需求改动:
   项目的md需要分开，因为角色不同，你需要新增一个Agent角色管理，
   新增一个角色管理，在项目管理的md就有对应角色的md管理，所以你还需要修改ui，弄成列表页，支持分页，点击查看在查看内容
   
   需求模块改动：
      需求模块只是一个大的模块，你需要有一个列表，用来显示子模块列表，子模块才是对应个字Agent需要负责的内容，而且需要有独立的步骤，也就是
      相同Agent需要按照自己的执行步骤执行

ai修复情况:
1. 已补充项目 Markdown 的角色维度，`project_markdown_file` 新增 `agent_role` 字段，支持按 `MAIN/DEVELOPER/TESTER/REVIEWER/OPS` 区分对应 Markdown。
2. 项目 MD 管理页已改为列表页：
   - 支持按角色筛选
   - 支持分页
   - 支持查看内容弹窗
   - 支持新增、编辑、删除后统一保存
   - 角色基础文件会自动落到 `roles/<role>/...` 路径下，通用文件继续保留根目录
3. 需求模块已增强为“总需求 + 子模块”结构展示：
   - 需求列表改为树形展示
   - 需求详情新增子模块列表
   - 子模块显示负责 Agent、当前阶段、独立执行步骤
4. 需求数据模型已新增 `execution_steps` 字段，创建需求时支持录入子模块执行步骤，满足“相同 Agent 按各自步骤执行”。
5. 验证情况：
   - 前端 `npm run build` 已通过
   - 后端当前环境无 `mvn` / `mvnw`，未能执行 Maven 测试，仅完成代码静态复核
