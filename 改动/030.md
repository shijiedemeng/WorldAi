前置md:
缺陷修复:
    
严重问题:
需求改动:
   文档库关联 切换为枚举和文档列表
   当前枚举有
     需求 AI 分析关联文档
     缺陷 AI 分析关联文档
      主控/开发 / 测试 / 运维 / 评审 /通同
     其中 需求和 缺陷 先有业务逻辑不动，只是切换存储方式，原先关联的地方你需要切换回来
      主控 / 开发 / 测试 / 运维 / 评审 / 通同 前面五个是在Agent初始化会话的是，自动根据Agent角色获取然后加载会话，但是都必须加载通同的文档
     另外子模块也新增文档选择，在任务下发的时候告诉Agent需要先阅读这个文档，当然也可以为空，默认是不填写
ai修复情况:
   已完成文档库关联模型调整：
   1. ProjectDocumentUsage 新增 Agent 会话文档用途枚举：
      - AGENT_COMMON：通用文档
      - AGENT_MAIN：主控文档
      - AGENT_DEVELOPER：开发文档
      - AGENT_TESTER：测试文档
      - AGENT_OPS：运维文档
      - AGENT_REVIEWER：评审文档
   2. 保留旧枚举 GENERAL_DOCUMENT / DEVELOPMENT_DOCUMENT 作为兼容入口，并在服务端归一化到 AGENT_COMMON / AGENT_DEVELOPER。
   3. 新增迁移 V24__document_usage_and_requirement_module_documents.sql：
      - 将旧 GENERAL_DOCUMENT 数据迁移为 AGENT_COMMON。
      - 将旧 DEVELOPMENT_DOCUMENT 数据迁移为 AGENT_DEVELOPER。
      - 迁移前会先清理可能导致唯一键冲突的重复旧数据。
      - requirement_module_info 新增 document_ids 字段，用于子模块关联文档。

   已完成项目文档关联页面调整：
   1. “文档库关联”改为按枚举列表动态展示，不再写死需求/缺陷/开发/通用四块。
   2. 当前页面支持配置：
      - 需求 AI 分析
      - 缺陷 AI 分析
      - 通用
      - 主控
      - 开发
      - 测试
      - 运维
      - 评审
   3. 需求 AI 分析、缺陷 AI 分析仍沿用原业务逻辑，只切换为新的统一存储/展示方式。
   4. 文档选择仍支持目录，服务端在使用时会自动展开目录下的全部子文档。

   已完成 Agent 会话初始化文档加载逻辑：
   1. python-client 创建会话时不再固定加载 GENERAL_DOCUMENT + DEVELOPMENT_DOCUMENT。
   2. 现在会固定加载 AGENT_COMMON，并根据 Agent 角色额外加载：
      - MAIN -> AGENT_MAIN
      - DEVELOPER -> AGENT_DEVELOPER
      - TESTER -> AGENT_TESTER
      - OPS -> AGENT_OPS
      - REVIEWER -> AGENT_REVIEWER
   3. project-development-documents 技能说明和脚本已同步更新为“通用 + Agent角色文档”模式。

   已完成子模块关联文档：
   1. 子模块新增 documentIds 字段。
   2. 普通子模块创建/编辑页面新增“关联文档”树形多选。
   3. 工作流子模块弹窗新增“关联文档”树形多选。
   4. 任务下发时，如果子模块配置了关联文档，服务端会把文档内容加入任务提示，要求 Agent 执行前先阅读并遵守。

   额外修正：
   1. MCP 文件检索提示现在可以正确兼容子模块编号，不再只按总需求查询。
   2. 子模块文档关联为空时不会影响任务下发。

   验证情况：
   1. frontend npm run build 通过。
   2. git diff --check 通过。
   3. 后端本地未编译：当前环境没有 mvn，也没有 mvnw。
