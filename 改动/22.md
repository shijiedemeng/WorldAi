前置md:
缺陷修复:
严重问题:
需求改动:
   项目管理新增 与文档库关联的地方，目前主要是两类，第一个是ai分析的关联的md，第二个是ai缺陷需要分析的时候关联的md，作用是关联之后
   在执行对应的时候，需要附带对于的文档内容，等于把文档内容带入到ai中进行分析
ai修复情况: 已完成项目与文档库的 AI 分析关联能力。后端新增 `project_markdown_document_link` 关联表、`ProjectDocumentUsage` 用途枚举、项目文档关联实体/仓库/DTO，并扩展项目 Markdown 配置接口，使 `/api/projects/{projectCode}/markdown-config` 可以同时保存原有本地 Markdown 文件配置和文档库关联配置。关联用途目前分为 `REQUIREMENT_AI_ANALYSIS` 和 `DEFECT_AI_ANALYSIS` 两类，保存时会校验关联的 Markdown 文档必须存在，并按选择顺序保存排序。项目 Markdown 服务新增 `buildAiContext(projectCode, usageType)`，会按项目和用途读取关联 Markdown 文档全文，生成可直接注入 AI Prompt 的上下文。需求 AI 拆分链路已在生成子模块 Prompt 时自动附带 `REQUIREMENT_AI_ANALYSIS` 关联文档内容；缺陷 AI 分析链路已在缺陷分析 Prompt 中自动附带 `DEFECT_AI_ANALYSIS` 关联文档内容。需求 AI 拆分已修正为只创建子模块，不再创建开发链路/执行链路，也不再要求 AI 返回 `linkType`。前端项目 Markdown 管理页新增“文档库关联”配置区，支持分别选择“需求 AI 分析关联文档”和“缺陷 AI 分析关联文档”，并提供跳转到文档库入口；文档选择会分页加载文档库全部文档，保存时随项目 Markdown 配置一起提交。前端类型和选项已同步扩展，`npm run build` 已验证通过；后端因当前环境缺少 `mvn` 且无 `mvnw`，未能执行 Maven 编译。
