前置md:
缺陷修复:
严重问题:
需求改动:
    修改技能 project-markdown-workspace 首先配置我新增了一个agentRole字段，也就是默认查看对应角色和通用的md
    还有拉取，默认是工作目录下的 md-files 的目录 然后在对应角色名称的子目录， 如果没有则创建
    各自的md拉取到对应角色的目录下
    技能还需要一个功能，就是初始化的时候需要默认加载这个目录下面的全部md文件，如果有更新或者拉取，需要重新加载对应的md
ai修复情况:
1. `project-markdown-workspace` 已支持从 [`.skill-config.json`](/Users/zhaoyiming/Desktop/项目/ai-api/skills/project-markdown-workspace/.skill-config.json) 默认读取 `agentRole`、`projectCode`、`url`，拉取时默认只处理“通用 + 当前角色”两类 Markdown。
2. 本地落盘目录已改为 `工作目录/md-files/`，并自动创建：
   - `md-files/common`
   - `md-files/<agentRole小写>`
3. 服务端通用 Markdown 会写入 `md-files/common/...`；当前角色的 Markdown 会写入 `md-files/<agentRole小写>/...`；其他角色默认不拉取。
4. 技能脚本已新增本地初始化/加载能力：
   - `init`：初始化并加载 `md-files/common` 与当前角色目录下全部 Markdown
   - `load`：重新加载本地 `md-files` 下的全部相关 Markdown
   - `sync`：拉取并写入后，会自动重新加载对应 Markdown
5. `sync` 仍保留 `CANCEL / OVERWRITE` 冲突策略，同时返回：
   - 写入文件
   - 跳过文件
   - 冲突文件
   - 重载文件
6. 技能文档已同步更新为新的 `agentRole + md-files + init/load/reload` 行为说明。
7. 验证情况：
   - `python3 skills/project-markdown-workspace/scripts/project_markdown_workspace.py --help`
   - `python3 skills/project-markdown-workspace/scripts/project_markdown_workspace.py config`
   - `python3 skills/project-markdown-workspace/scripts/project_markdown_workspace.py init`
   - `python3 skills/project-markdown-workspace/scripts/project_markdown_workspace.py load`
   - `python3 -m py_compile skills/project-markdown-workspace/scripts/project_markdown_workspace.py`
   均已通过本地验证
