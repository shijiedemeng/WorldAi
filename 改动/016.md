前置md:
   
缺陷修复:
严重问题:
需求改动:
    缺陷需要针对云效类型新增一个功能，就是项目不再手工填写，组织和令牌确认后自动拉取项目列表供选择
    接口参照：[016-3.md](016-3.md)
ai修复情况:
1. 已调整云效缺陷源基础信息保存逻辑：
   - 云效类型不再把项目 ID 绑定成创建流程，项目改为从自动拉取的列表中选择后再保存。
   - 禅道类型仍保持平台项目 ID 和项目名必填，避免影响原有配置方式。
2. 已新增云效获取项目列表能力：
   - 新增后端接口 `POST /api/defects/yunxiao/projects`。
   - 接口会使用当前表单里的云效服务地址、令牌和组织 ID，调用 `POST /oapi/v1/projex/organizations/{organizationId}/projects:search` 拉取项目列表。
   - 返回结果用于前端下拉选择和回填项目名称，不负责创建项目，也不改变云效侧数据。
3. 已补充前端交互：
   - 云效缺陷源表单中的“云效项目”改为下拉选择，项目列表会在选择组织后自动加载。
   - 新增“刷新项目列表”按钮，方便组织或令牌调整后重新拉取。
   - 原有“获取组织”逻辑保留，获取项目列表前必须先选择组织。
4. 已补充运行保护：
   - 查看云效缺陷前，如果项目还未选，会直接提示需要先从项目列表中选择，避免带空 `spaceId` 调云效接口。
5. 已验证：
   - 前端 `npm run build` 通过。
   - 当前环境没有 `mvn` 命令，后端 Java 编译无法在本机直接验证。
