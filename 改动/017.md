前置md:
缺陷修复:
严重问题:
需求改动:
   以下内容全部很对云效
   云效和对应前端的代码一定要独立开，因为后面还有其他平台，所以请务必保持独立
   缺陷状态还是有问题，[017-01.md](017-01.md) 你参考这个总的
   还有人员信息新增接口 [017-02.md](017-02.md) 补充上
ai修复情况:(这里需要ai处理之后自动填写修复内容)
1. 已按 `017-01.md` 补充云效缺陷状态字典：
   - 后端新增 `YunxiaoDefectStatusCatalog`，当云效工作项只返回状态 ID 时，会按字典回填中文状态名称。
   - 前端新增 `frontend/src/constants/yunxiaoDefect.ts`，云效缺陷状态下拉不再依赖当前页缺陷动态拼出来，而是使用完整状态列表。

2. 已按 `017-02.md` 补充云效项目成员接口：
   - 后端新增 `POST /api/defects/yunxiao/project-members`，支持按 `baseUrl/accessToken/organizationId/projectId/name/roleId` 查询云效项目成员。
   - 后端新增 `GET /api/defects/sources/{sourceCode}/yunxiao/members`，基于已保存的云效缺陷源自动读取组织 ID、项目 ID 和令牌，供缺陷列表页直接加载成员。

3. 已将云效前端筛选逻辑独立出来：
   - 新增 `frontend/src/views/defect/components/YunxiaoDefectFilterFields.vue`，专门处理云效状态、负责人、创建人、标签筛选。
   - 云效负责人和创建人改为项目成员下拉，选择值使用云效 `userId`，避免继续让用户手填 ID。
   - 通用缺陷列表页只负责按平台切换筛选组件，后续扩展其他平台时不需要继续把云效逻辑塞进通用表单。

4. 验证情况：
   - 前端 `npm run build` 已通过。
   - 当前环境没有 `mvn`，后端 Maven 编译无法在本机执行；已完成 Java 引用和接口路径静态检查。
