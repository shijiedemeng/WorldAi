前置md:
缺陷修复:
严重问题:
需求改动:
    新增一个模块，缺陷同步，目的是可以查看其他平台的缺陷，你需要用到中转技术，就是多个平台，但是在我们平台是统一输出的
    以列表形式，新增，首先选择账号密码，或者令牌，然后选择平台，选择对应的项目 点保存，然后点查看缺陷，看可以调用第三方平台看到对应的缺陷
    缺陷可能还需要兼容评论，因为有些缺陷可能会出现新的评论里
    目前先对接禅道，有一个技能可以参考一下(/Users/zhaoyiming/Desktop/项目/skills-测试/chandao)
    然后还需要新增一个同一的系统模块，系统模型先新增ai接口和模型设置，是否支持图片(部分缺陷可能有截图等情况)
    缺陷还有相关评论可以选择给，支持人工修改提交内容，提交给ai分析，分析的时候可以提交现在有哪些Agent，只提交对应项目的Agent 需要做什么事，然后可以看到结果，也可以点下一步，把任务推送到需求列表，ai分分析Agent需要做什么事，自动添加上对应Agent的子任务
    以上的完成了之后，在更新一下前端ui，简约大气一点，现在的不好看
ai修复情况:
1. 已按“列表配置 + 实时查看”重做缺陷模块，而不是预同步：
   - 缺陷源以列表形式维护
   - 每条配置包含平台、账号/令牌、项目、第三方项目标识
   - 列表后可直接点“查看缺陷”
   - 页面已改为真正的列表页，不再把新增配置、缺陷列表、详情、处理表单全部铺在一个大工作台里
   - 新增/编辑缺陷源改为弹窗；查看第三方缺陷改为抽屉，不影响主列表结构

2. 查看缺陷改为实时调第三方接口：
   - 当前先接禅道
   - 点击“查看缺陷”后，直接请求第三方平台接口并展示缺陷列表
   - 查看详情时同样走实时接口，支持展示评论
   - 查看行为本身不落本地库

3. 处理缺陷时才最小化保存本地信息：
   - 只有在提交 AI 分析时，才保存最小缺陷记录
   - 当前保存字段以缺陷 `id/编号/标题/状态/级别/摘要` 等基础信息为主
   - 不再要求先把远程缺陷整体同步入库
   - 已删除旧的同步式服务/DTO，避免继续误用“同步项目/同步账号”入口
   - 已新增迁移移除缺陷源里的 `sync_status` / `last_sync_time` 状态字段

4. AI 分析与推送流程保留：
   - 可人工编辑摘要和提示词
   - 分析时只允许选择当前项目的 Agent
   - 分析完成后可继续推送到需求列表

5. 系统设置模块保留：
   - 支持维护 AI 接口与模型配置
   - 支持是否可处理图片类缺陷

6. 验证情况：
   - 前端 `npm run build` 已通过
   - 当前环境没有 `mvn` / `mvnw`，未执行 Maven 编译与测试；后端仅完成静态代码检查
