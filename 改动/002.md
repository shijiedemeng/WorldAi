前置md:
缺陷修复:
严重问题:
需求改动:
Agent 列表缺少项目编号，看一下是页面没有显示还是什么，如果没有显示则需要显示，而且选择Agent的时候需要选择这个项目对应的Agent
如果代码里没有则补上这个业务
ai修复情况: 已排查，问题不是单纯前端漏显示，后端 Agent 模型与接口本身也没有 `projectCode` 字段，导致列表无法展示项目编号，需求页选择 Agent 时也无法按项目筛选。已补齐以下内容：
1. 后端新增 Agent 的 `projectCode` 字段，并在创建 Agent 时要求必须选择所属项目。
2. Agent 列表接口新增可选 `projectCode` 查询参数，返回结果包含 `projectCode`。
3. 前端 Agent 列表新增“项目编号”列，直接展示每个 Agent 的所属项目。
4. 前端新建 Agent 页面新增“所属项目”下拉，创建时会把项目编号一并提交。
5. 前端需求创建页调整为按当前 `projectCode` 拉取 Agent，下拉框只显示当前项目下的 Agent；切换项目时会自动刷新并清理不匹配的已选 Agent。
6. API 文档已同步更新 Agent 相关请求/响应字段说明。
