前置md:
缺陷修复:
严重问题:
需求改动:
   需求模块，我发现查看或者新增子需求的时候，没有返回上一层的按钮，你需要补上
   开发链路 执行回执需要有查看按钮，列表不显示，结果摘要也只显示部分，需要进到查看详情页面才能看到全部内容
ai修复情况:
  已处理。

  1. 子需求查看页补充“返回上一层”
     - 在需求详情页中，如果当前需求存在 `parentRequirementNo`，页头会显示“返回上一层”按钮
     - 点击后会返回父需求详情页

  2. 新增/编辑子需求页补充“返回上一层”
     - 当从父需求进入新建子模块页时，页头会显示“返回上一层”按钮
     - 编辑子需求时，如果该需求存在父需求，也会显示“返回上一层”按钮
     - 点击后回到父需求详情页

  3. 开发链路列表优化展示
     - 结果摘要改为列表内截断展示，只显示部分内容，避免表格过长
     - 新增“查看回执”按钮
     - 点击“查看回执”会弹出详情窗口，展示：
       - 需求编号
       - 链路类型
       - 任务标题
       - Agent
       - 状态
       - 完整结果摘要
       - 完整执行回执
       - 交付物路径

  4. 验证情况
     - 前端构建通过：`npm run build`
