# 系统架构流程说明

本文只说明四个服务之间的协作流程：Vue 前端、Java 后端、Python 客户端、向量服务。

## 一、服务角色

### Vue 前端

Vue 前端负责页面展示和人工操作入口。

主要职责：

- 展示项目、Agent、在线客户端、需求、子模块、工作流、执行记录、AI 日志、项目储备知识。
- 触发人工操作，例如创建需求、AI 分析、下发执行、查看会话、确认入库。
- 通过 HTTP API 调用 Java 后端。
- 通过后端推送通道接收实时状态变化，用于刷新页面状态和弹出提示。

### Java 后端

Java 后端是系统主控服务。

主要职责：

- 维护业务数据，例如项目、Agent 配置、需求、子模块、工作流、文档库、AI 日志、项目储备知识。
- 接收 Vue 前端请求并执行业务编排。
- 维护在线 Python 客户端和 Agent 的关联关系。
- 通过长连接向 Python 客户端下发会话创建、任务执行等命令。
- 接收 Python 客户端回传的会话状态、执行结果、收发记录和心跳。
- 调用外部 AI API 做需求拆分、缺陷分析、项目储备知识整理。
- 调用向量服务完成向量写入、检索和删除。

### Python 客户端

Python 客户端运行在实际工作目录所在机器上。

主要职责：

- 启动时读取本地客户端配置。
- 自动注册客户端节点，并按配置关联已有 Agent。
- 与 Java 后端保持长连接和心跳。
- 接收 Java 后端下发的会话创建和任务执行命令。
- 根据 Agent 类型调用本地 CLI / ACP / MCP 等工具。
- 在本地工作目录执行实际任务。
- 将执行状态、执行结果、会话收发记录回传给 Java 后端。

### 向量服务

向量服务是独立的向量存储访问层，当前可以使用 Milvus Lite，本质上后续也可以替换为独立 Milvus 服务。

主要职责：

- 接收 Java 后端传入的项目编号、向量、元数据和原文。
- 按项目编号隔离查询范围，避免不同项目知识互相污染。
- 写入项目储备知识向量。
- 根据查询向量检索相似知识。
- 删除指定知识向量。

## 二、整体交互关系

```text
Vue 前端
  |
  | HTTP API / 页面操作
  v
Java 后端
  |
  | 长连接命令 / 心跳 / 执行回传
  v
Python 客户端
  |
  | 调用本地 CLI / ACP / MCP / 文件系统
  v
本地 Agent 工作目录

Java 后端
  |
  | HTTP API
  v
向量服务
  |
  | Milvus Lite / Milvus
  v
向量库
```

Java 后端位于中心位置。Vue 前端不直接访问 Python 客户端，也不直接访问向量服务；Python 客户端也不直接操作前端页面。所有业务状态统一回到 Java 后端。

## 三、客户端启动和 Agent 关联流程

1. Python 客户端启动。
2. Python 客户端读取本地配置文件。
3. 配置文件里包含服务地址、客户端编码、客户端名称、已存在的 Agent 编号、Agent 工作目录等信息。
4. Python 客户端向 Java 后端注册客户端节点。
5. Java 后端检查配置中的 Agent 编号是否存在。
6. 如果 Agent 存在，则建立“客户端 - Agent”关联。
7. 如果 Agent 不存在，则不关联。
8. Python 客户端开始定时发送心跳。
9. Vue 前端在在线客户端、Agent 列表、会话管理等页面看到在线状态和心跳时间。

## 四、会话初始化流程

1. 用户在 Vue 前端打开需求详情或任务下发页面。
2. Vue 前端请求 Java 后端查询当前在线客户端、已关联 Agent、可用会话。
3. 如果没有可用会话，Java 后端向 Python 客户端下发创建会话命令。
4. Python 客户端收到创建会话命令后，按 Agent 配置进入对应工作目录。
5. Python 客户端初始化本地执行环境，例如加载技能、加载项目文档、准备 CLI / ACP 会话。
6. 初始化完成后，Python 客户端把会话 ID、运行类型、初始化结果回传 Java 后端。
7. Java 后端把会话状态缓存起来。
8. Vue 前端通过会话管理看到会话状态、工作目录、初始化结果和错误信息。

## 五、需求和子模块执行流程

1. 用户在 Vue 前端创建总需求。
2. 总需求可以是普通模式，也可以是工作流模式。
3. 用户可以手动创建子模块，也可以触发 AI 分析自动拆分子模块。
4. Java 后端保存子模块和工作流关系。
5. 用户点击下发执行，或者系统按工作流自动判断可执行节点。
6. Java 后端检查子模块状态、依赖关系、人工审核要求、Agent 在线情况和可用会话。
7. 如果子模块可以执行，Java 后端向对应 Python 客户端下发任务。
8. Python 客户端调用本地 Agent 工具执行任务。
9. 执行过程中，Python 客户端持续把实际发送内容、实际接收内容、状态变化回传给 Java 后端。
10. Java 后端更新子模块状态、执行记录、会话历史和工作流进度。
11. Vue 前端通过刷新或实时推送看到任务执行进度。

## 六、工作流执行流程

1. 工作流由多个子模块节点和依赖线组成。
2. 没有上游依赖，或者上游已经完成的节点，才允许进入可执行状态。
3. 如果多个节点可以同时执行，则进入并行执行判断。
4. Java 后端按 Agent 维度检查可用会话数量。
5. 如果默认会话空闲，则优先使用默认会话。
6. 如果并行任务数量大于可用空闲会话数量，则根据会话策略决定等待空闲会话或创建新会话。
7. 如果节点需要人工审核，则必须审核通过后才允许自动下发。
8. 子模块完成后，Java 后端继续检查下游节点是否满足执行条件。
9. 所有节点完成后，总需求进入完成状态。

## 七、AI 分析流程

1. 用户在 Vue 前端触发需求 AI 分析、缺陷 AI 分析或项目储备知识 AI 整理。
2. Vue 前端把操作请求发送给 Java 后端。
3. Java 后端组装业务上下文，例如需求内容、缺陷内容、可用 Agent、项目文档、执行回执。
4. Java 后端调用外部 AI API。
5. Java 后端把 AI 请求、响应、错误写入 AI 分析日志。
6. 如果是需求拆分，Java 后端根据 AI 返回结果创建子模块和工作流关系。
7. 如果是缺陷分析，Java 后端保存分析结果，供后续推送到需求。
8. 如果是项目储备知识整理，Java 后端把整理结果保存为可人工微调的内容。
9. Vue 前端展示处理状态和日志详情。

## 八、项目储备知识入库流程

1. 用户在需求详情中选择执行回执。
2. Vue 前端要求必须勾选至少一条回执。
3. Java 后端根据选中的回执生成原始材料。
4. 用户触发 AI 整理。
5. Java 后端异步调用 AI，把回执整理成项目储备知识。
6. 整理中，Vue 前端禁用选择、保存、确认入库等操作。
7. AI 整理完成后，用户可以人工微调确认内容。
8. 用户点击确认入库。
9. Java 后端使用向量配置调用 Embeddings API，把确认内容转换成向量。
10. Java 后端把项目编号、向量、元数据和原文发送给向量服务。
11. 向量服务写入 Milvus Lite 或 Milvus。
12. Java 后端更新知识状态为已入库。

## 九、向量检索流程

1. 用户或工具发起项目知识检索。
2. Vue 前端或 MCP 工具把项目编号、查询文本、向量配置发送给 Java 后端。
3. Java 后端先调用 Embeddings API，把查询文本转换成查询向量。
4. Java 后端把项目编号和查询向量发送给向量服务。
5. 向量服务只在指定项目范围内检索。
6. 向量服务返回相似知识列表。
7. Java 后端把结果返回给 Vue 前端或 MCP 调用方。

## 十、实时状态同步流程

1. Python 客户端定时向 Java 后端发送心跳。
2. Python 客户端执行任务时，把会话事件和执行结果回传给 Java 后端。
3. Java 后端更新内存会话状态和数据库业务状态。
4. Java 后端通过推送通道通知 Vue 前端。
5. Vue 前端收到推送后刷新对应页面或展示右下角通知。
6. 页面刷新后，历史业务状态从 Java 后端重新查询；仅页面缓存类通知刷新后丢失。

## 十一、核心原则

- Vue 前端只做展示和操作入口，不直接控制客户端和向量库。
- Java 后端是唯一业务编排中心。
- Python 客户端只负责本机执行和回传结果。
- 向量服务只负责向量写入、检索和删除，不管理业务流程。
- Agent 是否能执行，取决于 Java 后端记录的在线客户端、Agent 关联、工作目录、会话状态和任务状态。
- 项目知识向量检索必须带项目编号，避免跨项目命中。
